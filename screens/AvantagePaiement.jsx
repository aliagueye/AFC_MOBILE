import React from 'react';
import { View, Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function AvantagePaiement({item}) {
  return (
    <SafeAreaView>
      <Text> Paayer avec vos poinst avantages  </Text>
     </SafeAreaView>
  );
}
